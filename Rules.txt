{
  "rules": {
    "clients": {
      "$uid": {
        // Allow the user to read all data but write data only in their own node
        ".read": "auth != null",
        ".write": "auth != null && auth.uid === $uid"
      }
    }
  }
}
